import React from 'react';
import {LoadingWrap} from './Loading';

const Loading = () => {
    return (
        <LoadingWrap>
            <span></span>
        </LoadingWrap>
    );
};

export default Loading;